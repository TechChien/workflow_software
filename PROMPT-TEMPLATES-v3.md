# 需求分析工作流準則(v3)

> v3 重新設計於 2026-06-01,從「prompt 範本集」升級為「與模型無關的工作流準則」。
> v2 備份:[PROMPT-TEMPLATES-v2.md](./PROMPT-TEMPLATES-v2.md) ｜ v1 備份:[PROMPT-TEMPLATES-v1.md](./PROMPT-TEMPLATES-v1.md)

---

## 🧭 v3 為什麼跟 v2 不一樣

v2 是一疊「逐步 prompt」——**prompt 本身就是準則**。後果:換模型、或某個 prompt 寫法過時,整套就鬆動。

v3 把準則拆成兩層:

| 層 | 內容 | 換模型 / 換 agent 時 |
|---|---|---|
| **不變層(準則本體)** | 原則、關卡(含驗收標準)、產物契約 | **不動** |
| **實例層(可拋棄)** | 實際 prompt、用哪個 agent、措辭 | **隨時重寫** |

> **準則寫「要達成什麼、怎麼驗收」;prompt 只是「目前這個模型達成它的最佳寫法」。**
> prompt 全換掉,準則照樣站著 —— 這就是「不需要管執行的 AI 模型」的真正意思。

**讀法**:
- 要理解「為什麼這樣做」→ 讀 Part 0~3(不變層)。
- 要實際開跑、複製 prompt → 翻 Part 4 範本庫(實例層,允許過時)。

---

## 📜 Part 0:總則

### 0.1 七條原則(不變)

1. **描述 vs 決策分離** — 盤點只描述現況差異,不下判斷;決策獨立成關卡。
2. **內部決策 vs 業務上呈分離** — 工程自決與主管釐清分開記錄,避免主管 rubber-stamping 看不懂的技術選擇。
3. **意圖層 vs 實作層分離** — 需求只表達 WHAT/WHY;HOW(schema/欄位名/API 形狀)由工程在現況上接地產生,不由需求方發明。
4. **證據接地** — 任何「現況如何」的斷言都要附程式碼出處(檔案:行)。**新失敗模式是「很有自信但其實是錯的分析」,證據接地是唯一防線。**
5. **整合是關卡,不是步驟** — 不對齊就不能進開發。
6. **全程可追溯** — 釐清題(Q)、決策(D)、任務(T)互相編號引用,答案一改就能查出影響面。
7. **準則會自我進化** — 每個需求跑完留指標與教訓,回饋下一版準則。

### 0.2 兩種「需求與現況不符」(關鍵分辨)

| | 訊號型不符 | 雜訊型不符 |
|---|---|---|
| 是什麼 | 業務意圖 vs 現況的**真實落差** | 需求方工具(Gemini 等)瞎掰的 schema、欄位名、API 形狀 |
| 該怎麼辦 | **這就是需求本身,必須在 G2 被發現,不可消滅** | **G1 在源頭剝除** |

「讓 spec 一開始就符合專案」只能消滅雜訊型;訊號型不能消滅,因為**一份完全符合現況的 spec = 什麼都不用做**。讓開發順的不是「符合」,是 **spec 乾淨、意圖清楚、不發明假結構**。

### 0.3 每個關卡的四要素(不變層的格式)

```
入口(Entry)   :什麼前置產物就緒才能開始
產物(Artifact):這關必須生出什麼,存哪
驗收(Accept)  :怎麼判定這關「真的過了」(與模型無關的尺)
出口(Exit)    :過關後可進入哪一關
```

**驗收標準是 v3 的核心。** 不管哪個 AI 跑,輸出都要通過同一把尺。

---

## 🔀 Part 1:需求分級(進門先分流)

收到需求,先用下表定級,決定走幾關。**分級本身要記錄在需求檔開頭。**

| 級別 | 判準(任一成立即升級) | 走的關卡 |
|---|---|---|
| **Lite** | 單一團隊;不動 schema;不新增 API;< 1 人日 | G1 → G2 → G5(精簡)→ G7 |
| **Standard** | 單一團隊或輕度跨團隊;動既有 schema/API 但不新建表 | G1 → G2 → G3 → G5 → G7 |
| **Full** | 跨 BE/FE;新建表/新 API;牽涉法務/業務範圍判斷;> 3 人日 | G1 → … → G8 全走 |

判不準時**就高不就低**。分級可在 G2 盤點後**升級**(發現比想像複雜),但不可降級。

---

## 🚪 Part 2:關卡定義(準則心臟)

```
G1 意圖固化 ─→ G2 現況接地盤點 ─┬→ G3 決策分流 ─→ G4 主管理解簡報
                                │                         │(主管回覆)
                                └─────────────────────────┴→ G5 規劃
                                                              ↓
                                            G6 整合對齊 ✅ ─→ G7 交付 ✅ ─→ (開發)─→ G8 回顧校準
```
✅ = 強制關卡,不可省略(Full 級)。

---

### G1 — 意圖固化

**目的**:把需求方(主管 + Gemini)的原始輸入,剝成乾淨的「意圖層需求」,雜訊型不符在此清除。

- **入口**:拿到原始信件 / PRD / spec / 圖 / 附件。
- **產物**:`requirements\<YYYYMMDD>-<短代號>.md`,**只含意圖層**:
  - 要解決的問題、給誰用
  - 畫面/流程上要讓使用者看到或做到什麼(白話,非欄位名)
  - 業務規則、限制、明確不做(out-of-scope)
  - 原文 verbatim 保留一份(可追溯)
- **驗收**(同一把尺):
  - ☐ 全檔**無實作層內容**:無資料表、欄位名、API 端點、技術結構(那些是 HOW,留給 G2/G5)
  - ☐ 無「建議/應該/推薦」字眼
  - ☐ Gemini 發明的、專案不存在的名詞已標記為「需 G2 核對」或剝除
  - ☐ 原文與整理版並存
- **出口**:可進 G2。

> 雜訊型不符在這裡死掉;訊號型不符**故意留著**,等 G2 發現。

---

### G2 — 現況接地盤點(護城河)

**目的**:純描述「意圖 vs 現況」的真實落差。**這是整個工作流投報最高、也最容易出「自信的錯」的一關。**

- **入口**:G1 產物就緒。
- **產物**:`plans\<BE|FE>-gap-<短代號>.md`(跨團隊則各一份),只含:
  1. 資料欄位對照(意圖要呈現什麼 → 現有 entity/type)
  2. API/端點對照(需要什麼 → 現有可重用什麼)
  3. 資料表/元件評估(可重用 / 需擴充 / 需新建)
  4. 既有規格檢視(若需求帶 SQL/規格,做語法/約定對照)
  5. 權限/安全現況描述
- **驗收**(同一把尺,**最嚴**):
  - ☐ **每一條落差都附程式碼出處(檔案:行)**;無出處的斷言一律標「未驗證」
  - ☐ 抽查 3 條引用 → 實際打開檔案核對為真,才算過關
  - ☐ 每條差異標明性質:完全對應 / 部分對應 / 缺漏 / 衝突
  - ☐ **零判斷**:無「建議/推薦/應該怎麼做」、無待確認題清單(留 G3)
  - ☐ 若盤點後發現複雜度超出分級 → 在此升級需求等級
- **出口**:Standard/Full 進 G3;Lite 直接進 G5。

---

### G3 — 決策分流

**目的**:把所有待決事項分成「工程自決」與「上呈主管」兩流,分別歸檔。

- **入口**:G2 gap 就緒。
- **產物**:
  - `plans\decisions-<短代號>.md` — 工程內部決策(技術 lead 自決)
  - 標記為「→ 上呈 G4」的項目清單(尚未作答)
- **分流判準**:
  - **工程自決(寫進 decisions)**:schema 設計、命名 convention、演算法、函式庫版本、UI 元件選擇、視覺細節、內部 DTO 形狀。
  - **上呈主管(交給 G4)**:答案需要**業務 / 法務 / 時程**判斷,而非技術判斷。
  - **混合題**(常見且最難):拆成兩半 —— 技術面進 decisions,業務面標上呈。例:「JSON 欄位 vs 拆欄」技術選擇自決,但「未來要不要支援第三方匯入」是業務,上呈。
- **decisions 格式**(每題附 ID 與 why):
  ```
  ## D-XX <決策標題>
  - 選擇:<方案>
  - 理由:<為什麼>
  - 替代方案:<排除了什麼 + 為什麼不選>
  - 影響範圍:<影響哪些 T-XX / 檔案>
  - 來源:<derives 哪條 gap / Q-XX>
  ```
- **驗收**:
  - ☐ 每個待決事項都被分到「自決」或「上呈」其一,無遺漏
  - ☐ 每筆 decision 有 D-XX 編號、有 why、有替代方案
  - ☐ 上呈項目不在 decisions 裡作答(避免越權)
- **出口**:進 G4(若有上呈項)或直接進 G5(若全自決)。

---

### G4 — 主管理解簡報(v2 缺的一層)

**目的**:給主管一份能**理解**(不只回答問題)的綜合簡報。它是 G2 gap 的**業務翻譯與綜合**,**不是把 gap 原文丟給主管**(那是 v1 翻車主因)。

- **入口**:G3 完成,有上呈項目。
- **產物**:`plans\briefing-<短代號>.md`,結構(SCQA / 金字塔):
  1. **你要的** — 需求一段話(請主管確認沒誤解)
  2. **我們已經有的** — 可重用部分(讓主管看到沒在重造輪子)
  3. **缺的 / 要新做的** — 真正工作量(gap 的業務翻譯)
  4. **風險與不確定**
  5. **要主管拍板的選項** — 每題:業務影響(非技術影響)+ 2~3 個取捨選項(勾選框)+ 推薦答案與理由(主管驗得了的論述)+ 不釐清的風險
  6. **一張圖** — 現況 vs 目標差異
- **只准問三類**:✅業務範圍 ✅法務風險 ✅時程取捨。**禁止**問 schema/命名/型別/函式庫/UI 元件/演算法邊界(那些是 G3 自決)。
- **驗收**:
  - ☐ 全篇業務語言,主管讀得懂、驗得了
  - ☐ 每個結論在附錄可回指 G2 gap 的某條證據(結論層 / 證據層雙視角)
  - ☐ 每個待答題附 Q-XX 編號、選項、推薦、風險
  - ☐ 結尾有簽核欄位
- **出口**:寄出。**同時**進 G5(見下「不阻塞」規則)。

> **不阻塞規則**:G4 寄出後不要乾等主管。簡報每題附「預設假設」,G5 先照假設規劃,並標出哪些 T-XX 依賴哪個 Q-XX。主管答案回來(`clarification-answers-<短代號>.md`),只修受影響的 task。把阻塞式等待變成可平行的風險。

---

### G5 — 規劃(BE / FE)

**目的**:產出可交付給工程師的實作藍圖。

- **入口**:G2 完成(Lite);或 G3 完成 + G4 假設/答案就緒(Standard/Full)。
- **(Full 跨團隊建議)契約先行**:BE/FE plan 前,先合寫一份共享契約 `plans\contract-<短代號>.md`(endpoint / DTO / enum / 錯誤碼),兩邊都 plan against 它。**事前對齊勝過 G6 事後找碴。**
- **產物**:`plans\<BE|FE>-plan-<短代號>.md`:
  1. WBS(任務 T-XX / 工時 / 相依 / 完成定義)
  2. 資料庫變更(DDL / Seed / **Rollback / 既有資料 backfill**)
  3. API 規格(Request/Response/權限/錯誤碼/範例)
  4. 核心邏輯規格
  5. **非功能 checklist**:資料遷移、安全邊界、效能、可觀測性、a11y/i18n(不適用也要寫「不適用」)
  6. 風險與緩解
  7. 時程對照 deadline(標 must-have / 可延後)
  8. 對本專案 CLAUDE.md 約定的遵守檢查表
- **驗收**:
  - ☐ 每個 T-XX 標 `derives: D-XX / Q-XX`(可追溯)
  - ☐ 已定案的 D-XX 直接照辦,不重開討論
  - ☐ 非功能 checklist 每項有答(含「不適用」)
  - ☐ 工時標註「僅供相對排序,非交付承諾」
  - ☐ 不引入新 dependency(若引入須說明理由);不夾帶 scope creep / 既有違規重構
- **出口**:Lite 進 G7;其他進 G6。

---

### G6 — 整合對齊(強制關卡)

**目的**:確認 BE/FE plan 對齊;若已做契約先行,本關降為「契約遵守確認」。

- **入口**:BE-plan 與 FE-plan 都就緒。
- **對齊 checklist**(逐項 cross-reference):
  - ☐ Endpoint 路徑(完整字串比對)☐ HTTP method
  - ☐ Request/Response DTO 欄位名 + 型別
  - ☐ 欄位特殊處理(typo 保留 / 大小寫慣例)
  - ☐ Enum 值域兩邊完整一致 ☐ Null vs 空字串語意
  - ☐ 錯誤碼 ☐ i18n key 對應 BE enum
- **產物**:`plans\INTEGRATED-<短代號>.md`:每項 ✅/❌、衝突清單(具體位置+兩邊差異)、修正方案(誰改/改哪/為何)、整合測試清單(必跨團隊測的情境)。
- **驗收 / 關卡規則**:
  - ☐ **全部 ✅ 才可進 G7**;有 ❌ → 回 G5 修正 → 重跑 checklist
  - ☐ 衝突的修正已寫回原 plan(不是只記在 INTEGRATED)
- **出口**:全 ✅ 進 G7。

---

### G7 — 交付(強制關卡)

**目的**:標準化打包,工程師看一份就能開工。

- **產物**:`plans\<BE|FE>-handoff\`:
  ```
  ├── <BE|FE>-plan-<短代號>.md   ← 主藍圖
  ├── decisions-<短代號>.md       ← why
  ├── INTEGRATED-<短代號>.md      ← 已對齊事項
  ├── contract-<短代號>.md        ← 共享契約(若有)
  ├── requirements\<需求檔>.md    ← 原始需求
  ├── 原始附件、diagrams\*.svg
  └── handoff-prompt.md           ← 啟動 prompt(實例層,見 Part 4)
  ```
- **不交付**:briefing(主管決策已內化進 plan/decisions)、早期 gap(已被 plan 涵蓋,作附件即可)。
- **交付前 checklist**:
  - ☐ INTEGRATED 全 ✅ ☐ 所有「待整合確認」已修正
  - ☐ handoff-prompt 無過時引用 ☐ diagrams 已更新(若 schema 異動)
  - ☐ 原始附件路徑可存取 ☐ T-XX ↔ D-XX ↔ Q-XX 串接完整
- **出口**:進開發。

---

### G8 — 回顧校準(讓準則自我進化)

**目的**:每個需求跑完,留下數據與教訓,回饋準則本身。**這是 v3 跟一次性 SOP 的差別。**

- **入口**:需求開發完成 / 上線。
- **產物**:`plans\retro-<短代號>.md`,記錄四個指標:
  - **衝突外洩數** — G6 標 ✅ 後,開發期仍爆出的整合衝突(理想 = 0)
  - **釐清往返次數** — G4 來回幾趟主管才定案
  - **估時誤差** — WBS 工時 vs 實際
  - **假設命中率** — G4 預設假設後來被主管推翻的比例
- **驗收**:
  - ☐ 四指標都有數字
  - ☐ 至少一條「下版準則該改什麼」的具體建議(寫進本檔 Part 6 候選清單)
- **出口**:建議併入準則 → 版本遞增。

---

## 📦 Part 3:產物契約與 ID 串接

### 3.1 ID 命名(全程可追溯)

| 前綴 | 意義 | 產生於 |
|---|---|---|
| `Q-XX` | 上呈主管的釐清題 | G4 |
| `D-XX` | 工程內部決策 | G3 |
| `T-XX` | 開發任務 | G5 |

**串接規則**:`T-XX` 必標 `derives: D-XX / Q-XX`;`D-XX` 必標來源 gap 條目或 `Q-XX`。
→ 主管哪天改某個 Q 的答案,可立刻 grep 出受影響的 D 與 T,做影響分析。

### 3.2 檔案組織

```
requirements/
├── PROMPT-TEMPLATES.md              ← 本檔(v3 準則)
├── PROMPT-TEMPLATES-v2.md / -v1.md  ← 舊版備份
├── <YYYYMMDD>-<短代號>.md           ← G1
└── plans/
    ├── <BE|FE>-gap-<短代號>.md       ← G2
    ├── decisions-<短代號>.md         ← G3
    ├── briefing-<短代號>.md          ← G4
    ├── clarification-answers-<短代號>.md ← 主管回覆
    ├── contract-<短代號>.md          ← G5(選用)
    ├── <BE|FE>-plan-<短代號>.md      ← G5
    ├── INTEGRATED-<短代號>.md        ← G6
    ├── <BE|FE>-handoff/              ← G7
    ├── retro-<短代號>.md             ← G8
    └── diagrams/*.svg
```

### 3.3 短代號規則

kebab-case,≤ 20 字,給檔名用。例:`sbom`、`batch-reboot`、`oauth-login`。

---

## 🧩 Part 4:實例 — prompt 範本庫(可拋棄層)

> ⚠️ **本段可過時。** 這些只是「目前的模型,達成 Part 2 各關驗收的最佳寫法」。
> 換模型 / 換 agent 時,只改本段;Part 0~3 不動。
> 每個 prompt 的成敗標準,以對應關卡的「驗收」為準,不以 prompt 措辭為準。

### 通用前言(每個 prompt 都該含)

```
【Mockup / 外部 spec 是方向參考,不是規格】
- Gemini Canvas / Figma 等產出僅供方向參考
- 視覺風格 / 元件位置 → 以本專案既有設計系統為準
- 欄位名稱 / 表 schema / API 形狀 → 以本專案實際程式碼為準
- 衝突時本專案優先;若為刻意改版,記成一筆 D-XX 再覆蓋(留逃生門)
- 不要為對齊 mockup 而違反專案約定
```

### G1 prompt(意圖固化)

```
讀原始資料:信件 <path>、附件 <path 列表>。
整理成中性意圖層 md,存 requirements\<YYYYMMDD>-<短代號>.md。
內容:來源資訊 / 信件 verbatim / 附件整理 / 統計標註。
嚴格禁止:建議推薦字眼、任何實作層內容(表/欄位名/API/技術結構)、
          mockup 合理性評論、待確認題清單。
Gemini 發明而專案可能不存在的名詞 → 標「需 G2 核對」,不要當成事實。
```

### G2 prompt(現況接地盤點)

```
讀:1) requirements\<需求檔>.md  2) 用 codebase-onboarding-engineer 讀相關
   entity/controller/service/DDL(BE)或 view/component/type/i18n(FE)。
產出「意圖 vs 現況」差異對照,存 plans\<BE|FE>-gap-<短代號>.md。
每一條落差【必附程式碼出處(檔案:行)】;查不到出處的標「未驗證」。
每條標性質:完全對應/部分對應/缺漏/衝突。
明確禁止:待確認題、建議答案、實作策略。
（agent 可換;驗收以 G2 那把尺為準)
```

### G3 prompt(決策分流)

```
讀:需求檔 + gap 檔。
列出所有待決事項,逐筆分流:技術判斷→寫進 plans\decisions-<短代號>.md(D-XX 格式,附 why+替代方案+影響範圍+來源);
業務/法務/時程判斷→標「→ 上呈 G4」,不在此作答。
混合題拆兩半。
```

### G4 prompt(主管理解簡報)

```
讀:需求檔 + decisions 中標「→ 上呈」的 Q 項目 + gap(僅作證據回指,不照搬)。
寫 plans\briefing-<短代號>.md,SCQA 結構(你要的/已有的/要新做的/風險/待拍板選項/一張圖)。
業務語言;每題 Q-XX:業務影響 + 2-3 取捨選項(勾選) + 推薦+理由 + 不釐清風險 + 預設假設。
每個結論附錄回指 gap 證據。結尾簽核欄。
禁問:schema/命名/型別/函式庫/UI 元件/演算法邊界。
```

### G5 prompt(規劃)

```
讀:需求檔 + gap + decisions(已定案直接照辦) + clarification-answers(或用 briefing 預設假設先做)。
（Full 跨團隊:先合寫 plans\contract-<短代號>.md)
產出 plans\<BE|FE>-plan-<短代號>.md:WBS(T-XX,標 derives) / DDL+Rollback+backfill /
   API 規格 / 核心邏輯 / 非功能 checklist / 風險 / 時程 / CLAUDE.md 遵守表。
工時標「僅供相對排序」。不 scope creep、不重構既有違規。
依賴主管假設的 task 標出對應 Q-XX。
```

### G6 prompt(整合對齊)

```
讀:BE-plan + FE-plan(+ contract 若有)。
逐項跑對齊 checklist(endpoint/method/DTO/enum/null語意/錯誤碼/i18n)。
產出 plans\INTEGRATED-<短代號>.md:每項 ✅/❌、衝突清單、修正方案、整合測試清單。
有 ❌ → 寫回原 plan 修正 → 重跑,全 ✅ 才結束。
```

### G7 handoff-prompt.md(交給工程師的啟動 prompt)

```
我要開發 <短代號> 功能。請先讀(優先序):
1) <BE|FE>-plan(主藍圖) 2) decisions(理由+替代) 3) INTEGRATED(已對齊) 4) 本專案 CLAUDE.md
約束:
- 嚴格遵守 CLAUDE.md;不主動重構既有違規程式
- 每完成一個 T-XX 給 commit + 簡短 status,我確認再下一個
- plan 沒寫的決策:屬技術內部→即時決策並補進 decisions;屬業務→暫停問我
請從 T0 開始。
```

### Agent 選擇真相(去神秘化,屬實例層)

```
事實:~/.claude/agents/*.md 是 persona prompt 模板,底層同一個模型。
價值:強制多角度 / 結構化輸出 / 領域 framework 喚起。
不帶來:更高推理力 / 平行運算 / 取代好的 prompt 內容。
建議:agent 屬「可拋棄實例層」,愛換就換;心力放在 prompt 的「請考慮 X/Y/Z」清單與關卡驗收,不在 agent 名字。沒匹配的就直接寫好 prompt。
```

### 業務 vs 技術語言翻譯(G4 用)

| 技術措辭 | 業務措辭 |
|---|---|
| DTO 欄位型別 | 報告呈現的資料格式 |
| Parser 演算法邊界 | 風險評估覆蓋哪些情境 |
| JSON 欄位 vs 拆欄 | 未來結構演化彈性 vs 查詢效能 |
| API 命名 / schema 索引 / cache 策略 | ❌ 不該問主管(G3 自決) |
| 視覺顏色 token | ❌ 該問設計師 |

通用模板:不問「選 A 還 B」,改問「客戶/使用者/法務 會看到什麼差異?」

---

## 🗂️ Part 5:速查 — 我該在哪一關?

| 你手上的東西 | 你在 | 下一步 |
|---|---|---|
| 主管 + Gemini 的原始 PRD/圖 | 進 G1 前 | 先 Part 1 定級 |
| 一份乾淨意圖層需求 | G1 完成 | G2 接地盤點 |
| 帶程式碼出處的 gap | G2 完成 | G3 分流(或 Lite→G5) |
| decisions + 上呈清單 | G3 完成 | G4 寫簡報 |
| 主管簽核回來 | G4 完成 | G5 修受影響 task |
| BE-plan + FE-plan | G5 完成 | G6 對齊 |
| INTEGRATED 全 ✅ | G6 完成 | G7 打包 |
| 功能上線 | 開發完成 | G8 回顧 |

---

## 🎓 Part 6:未來改進清單(v4 候選)

- [ ] G6 對齊 checklist 自動化(grep / 腳本比對 contract)
- [ ] G8 四指標跨需求彙總成趨勢儀表板
- [ ] contract-<短代號>.md 的機器可讀格式(OpenAPI / JSON Schema),讓 G6 真正可程式驗證
- [ ] Lite 級的「單檔合併產物」(gap+plan 合一,再省關卡)
- [ ] handoff-prompt 隨專案類型動態生成
- [ ] G2 證據接地的自動抽查腳本(隨機挑引用核對檔案:行是否存在)
