services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: workflow_software
      POSTGRES_USER: workflow
      POSTGRES_PASSWORD: workflow
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
